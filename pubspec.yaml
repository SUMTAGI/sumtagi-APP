name: ic_island_app
description: "인천 섬 여행 앱"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.6.3
  shared_preferences: ^2.3.2
  cached_network_image: ^3.4.1
  flutter_animate: ^4.5.0
  url_launcher: ^6.3.1
  supabase_flutter: ^2.8.4
  flutter_dotenv: ^5.2.1
  http: ^1.2.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
